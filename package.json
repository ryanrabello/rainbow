{
  "name": "pride",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "yarn build --base=/rainbow/ && touch dist/.nojekyll && git add dist/ && git commit -m \"Deploy gh-pages\" && git subtree push --prefix dist origin gh-pages"
  },
  "dependencies": {
    "d3": "^7.4.4",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "simplex-noise": "^3.0.1"
  },
  "devDependencies": {
    "@types/d3": "^7.4.0",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^1.3.0",
    "typescript": "^4.6.3",
    "vite": "^2.9.9"
  }
}
